pipeline {
     agent {
         node {
             label 'maven-agent'
         }
     }     
     stages {
         stage('Build') {
             steps {
                echo "printenv"
             }
         }
     }    
}